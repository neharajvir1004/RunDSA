<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rundsa.app">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunDSA"
        android:usesCleartextTraffic="true">

        <!-- Splash (Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activities.LoginActivity" android:exported="false" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" />

        <!-- Main -->
        <activity android:name=".activities.HomeActivity" android:exported="false" />

        <!-- Learning -->
        <activity android:name=".activities.LearnActivity" android:exported="false" />
        <activity android:name=".activities.TopicDetailActivity" android:exported="false" />

        <!-- Quiz -->
        <activity android:name=".activities.QuizActivity" android:exported="false" />
        <activity android:name=".activities.QuizLevelActivity" android:exported="false" />
        <activity android:name=".activities.QuizQuestionActivity" android:exported="false" />
        <activity android:name=".activities.QuizResultActivity" android:exported="false" />

        <!-- Practice -->
        <activity android:name=".activities.PracticeCodeActivity" android:exported="false" />
        <activity android:name=".activities.PracticeDetailActivity" android:exported="false" />

        <!-- Fun -->
        <activity android:name=".activities.FunExerciseActivity" android:exported="false" />
        <activity android:name=".activities.SortingGameActivity" android:exported="false" />
        <activity android:name=".activities.BinarySearchGameActivity" android:exported="false" />
        <activity android:name=".activities.MemoryGameActivity" android:exported="false" />
        <activity android:name=".activities.ReverseGameActivity" android:exported="false" />

        <!-- Progress -->
        <activity android:name=".activities.ProgressActivity" android:exported="false" />

        <!-- Compiler -->
        <activity android:name=".activities.CompilerActivity" android:exported="false" />

    </application>

</manifest>